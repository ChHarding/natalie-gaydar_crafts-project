# natalie-gaydar_crafts-project
Tool that allows users to browse and filter crafts based on category and popularity.

Now in the process of converting to a Streamlit app. Should be able to be run with streamlit run main.py if you have streamlit installed locally or ".venv/Scripts/streamlit.exe" run main.py if it's installed in your venv.